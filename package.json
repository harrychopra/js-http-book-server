{
  "name": "js-http-book-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest",
    "create-db": "docker exec -it pg14 psql -c 'create database book_server;'",
    "drop-db": "docker exec -it pg14 psql -c 'drop database book_server;'",
    "migrate-up": "docker exec -i pg14 psql < ./db/migrations/up.sql",
    "migrate-down": "docker exec -i pg14 psql < ./db/migrations/down.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "node-postgres": "^0.6.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.11"
  }
}
